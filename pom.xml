<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.redcoffee</groupId>
  <artifactId>RCPuttPutt</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>jar</packaging>
  <name>RCPuttPutt</name>
  <url>https://github.com/CoffeePNG/RCPutt</url>

  <developers>
    <developer>
      <id>CoffeePNG</id>
      <name>Nick Merzon</name>
      <email>nickmerzon@gmail.com</email>
      <organization>RepubliCraft</organization>
      <roles>
        <role>author</role>
        <role>maintainer</role>
      </roles>
    </developer>
  </developers>
  <description>Playable putt-putt with rolling-ball physics, configurable surfaces, party rounds and leaderboards.</description>

  <properties>
    <java.version>25</java.version>
    <maven.compiler.release>25</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <purpur.version>26.2.build.2627-stable</purpur.version>
  </properties>

  <repositories>
    <repository>
      <id>papermc</id>
      <url>https://repo.papermc.io/repository/maven-public/</url>
    </repository>
    <repository>
      <id>purpurmc</id>
      <url>https://repo.purpurmc.org/snapshots</url>
    </repository>
  </repositories>

  <dependencies>
    <dependency>
      <groupId>org.purpurmc.purpur</groupId>
      <artifactId>purpur-api</artifactId>
      <version>${purpur.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <!-- RCUI (and the RCPlatform it sits on) are third-party: part of Bobo's shared RC framework,
           not ours. Consumed through their published API only - never forked or vendored here.
           provided, because RCUI ships these classes in its own plugin jar at runtime. -->
      <groupId>net.republicraft</groupId>
      <artifactId>RCUI</artifactId>
      <version>3.0.0</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>net.republicraft.platform</groupId>
      <artifactId>rcplatform-api</artifactId>
      <version>1.0.0</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <!-- RCParties shades these classes into its own jar and ships them at runtime, so this MUST
           stay provided: shading them a second time causes LinkageError. Only RCParties.jar goes
           in plugins/, never the API jar. -->
      <groupId>gg.rc</groupId>
      <artifactId>rcparties-api</artifactId>
      <version>1.0.0-SNAPSHOT</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.xerial</groupId>
      <artifactId>sqlite-jdbc</artifactId>
      <version>3.46.1.3</version>
      <!-- Not shaded: plugin.yml declares it under libraries: so Paper resolves it at load time.
           Shading is also not an option here - the shade plugin's ASM cannot read Java 25 bytecode. -->
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.11.3</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>${project.name}-${project.version}</finalName>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>true</filtering>
        <includes><include>paper-plugin.yml</include></includes>
      </resource>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>false</filtering>
        <excludes><exclude>paper-plugin.yml</exclude></excludes>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.2</version>
      </plugin>
    </plugins>
  </build>
</project>
