# RCPuttPutt - RC-SPEC-PUTTPUTT-001
# Everything a course designer needs to tune lives here. Surfaces are a registry, so new hazards,
# bumpers and pads are config entries rather than code.

physics:
  # Blocks per tick. MUST stay below 1.0: a ball that travels more than a block between samples can
  # step straight over a 1-block wall without ever testing it. This cap is the tunneling guard.
  max_velocity: 0.9
  # Below this speed the ball stops and the next stroke is allowed.
  rest_epsilon: 0.02
  # Over the cup faster than this and the ball lips out instead of dropping.
  max_sink_speed: 0.25
  # Blocks from the cup centre that count as "over the cup".
  sink_radius: 0.35
  # Velocity of a fully drawn putt. Clamped to max_velocity.

surfaces:
  green:
    friction: 0.92          # baseline roll
  sand:
    friction: 0.75          # heavy drag - the ball bogs down and can get stuck
  ice:
    friction: 0.99          # slick - overshoot risk
  wall:
    type: wall
    restitution: 0.70       # bounce energy retained
  water:
    type: hazard
    penalty: 1              # strokes added on entry
    reset: last_rest        # last_rest | tee
  booster_north:
    type: impulse
    impulse:
      direction: [0, 0, -1] # world-space vector; normalised on load
      strength: 0.35
  booster_south:
    type: impulse
    impulse:
      direction: [0, 0, 1]
      strength: 0.35
  booster_east:
    type: impulse
    impulse:
      direction: [1, 0, 0]
      strength: 0.35
  booster_west:
    type: impulse
    impulse:
      direction: [-1, 0, 0]
      strength: 0.35
  cup:
    type: hole
  # Rivers: a wide, sustained flow. preventRest keeps the ball drifting instead of parking
  # mid-stream - it only settles once it leaves the current.
  river_east:
    type: current
    current:
      direction: [1, 0, 0]
      strength: 0.12
    preventRest: true
  river_north:
    type: current
    current:
      direction: [0, 0, -1]
      strength: 0.12
    preventRest: true

# Block material -> surface id. Per-course overrides live in the course file and win over this map.
# Anything unmapped rolls like plain green.
material_map:
  GREEN_CONCRETE: green
  SAND: sand
  BLUE_ICE: ice
  SMOOTH_STONE: wall
  WATER: water
  LIGHT_BLUE_CONCRETE: booster_north
  ORANGE_CONCRETE: booster_east
  CYAN_CONCRETE: river_east
  LIGHT_GRAY_CONCRETE: river_north

# Ball-to-ball collision. Affordable because play is turn-based: only one ball is ever under power,
# so a strike resolves against resting balls rather than an n-body simulation.
ball-collision:
  enabled: true
  restitution: 0.85       # fraction of normal velocity handed to the struck ball
  radius: 0.18            # ball radius in blocks; contact happens at twice this
  allow-knock-in: true    # false = purists; a knocked-in ball is put back instead of counting

turn-order:
  # ascending  = lowest total putts FIRST. This is a deliberate penalty: the leader reads the green
  #              for everyone with no information of their own, which keeps rounds close.
  # descending = reward the leader instead.
  mode: ascending

shot-clock:
  seconds: 30
  timeout-penalty: 1
  max-consecutive-timeouts: 3   # after this many forfeits in a row, the player is capped out

# Hard cap so one player cannot stall a hole forever.
max-strokes-per-hole: 10

power-meter:
  # oscillate = bar sweeps 0 -> 100 -> 0 while held; release locks it in (timing skill).
  # fill      = bar rises to full and holds; release fires at the current value.
  mode: oscillate
  sweep-ticks: 30         # ticks for one 0 -> 100 pass
  min-velocity: 0.08
  max-velocity: 0.9       # clamped to physics.max_velocity

snapshot:
  interval-seconds: 15
  resume-window-minutes: 10

out_of_bounds:
  # Strokes added when a ball leaves the hole's bounds.
  penalty: 1

leaderboard:
  size: 10

economy:
  # v1 seam only. Entry fees and payouts are stubbed until the Vault integration lands.
  enabled: false

items:
  ball:
    material: SNOWBALL
    name: '<white>Golf Ball</white>'
    # custom_model_data, NOT item_model: LabyMod and some other clients render custom_model_data
    # reliably but ignore item_model overrides.
    custom_model_data: 1001.0
  putter:
    # A shovel, not a bow: the plugin owns the charge curve via the boss bar power meter.
    material: IRON_SHOVEL
    name: '<gold>Putter</gold>'
    custom_model_data: 1002.0

messages:
  prefix: '<dark_green>[<green>PuttPutt</green>]</dark_green> '
  putt:
    stroke: '<gray>Hole <white><hole></white> - stroke <white><strokes></white>.</gray>'
    still-rolling: '<red>Your ball is still rolling.</red>'
    no-round: '<red>You are not in a round. Try /puttputt start <course>.</red>'
    cannot-drop: '<red>You cannot drop your putter mid-round.</red>'
    sunk: '<green><result>!</green> <gray>Hole <white><hole></white> in <white><strokes></white> (par <par>).</gray>'
    knocked-in: '<green>Knocked in!</green> <gray>Someone put you in the cup on hole <white><hole></white> for <white><strokes></white>.</gray>'
    hazard: '<red>In the <surface>!</red> <gray>+<penalty> stroke(s).</gray>'
    out-of-bounds: '<red>Out of bounds!</red> <gray>+<penalty> stroke(s).</gray>'
  power:
    title: '<yellow><player>''s power: <white><percent>%</white></yellow>'
  turn:
    yours: '<green>Your turn!</green> <gray>Hold right-click to charge. <white><seconds>s</white> on the clock.</gray>'
    other: '<gray>Waiting on <white><player></white>...</gray>'
    not-yours: '<red>It is not your turn.</red>'
    order: '<gray>Order this hole: <white><order></white></gray>'
    clock: '<red><seconds></red><gray>s left</gray>'
    timeout: '<red>Out of time!</red> <gray>+<penalty> stroke(s).</gray>'
    capped: '<red>Maximum <strokes> strokes on this hole.</red>'
  round:
    started: '<green>Round started on <white><display></white></green> <gray>- <holes> holes, par <par>.</gray>'
    hole-start: '<yellow>Hole <white><hole></white>, par <white><par></white>.</yellow>'
    player-finished: '<green>Round finished!</green> <gray><strokes> strokes (<diff>).</gray>'
    complete: '<green>Your round on <white><course></white> is over.</green>'
    left: '<gray>You left the round.</gray>'
    resumed: '<green>Resumed your round on <white><course></white></green> <gray>at hole <white><hole></white>.</gray>'
    not-playing: '<red>You are not in a round.</red>'
    already-playing: '<red>You are already in a round.</red>'
    not-leader: '<red>Only the party leader can do that.</red>'
    party-busy: '<red>Your party is already in another activity.</red>'
    member-busy: '<red><player> is already in a round.</red>'
    no-members: '<red>Nobody in your party is online.</red>'
    world-missing: '<red>That course''s world (<world>) is not loaded.</red>'
    course-incomplete: '<red>Course <course> has unfinished holes: <holes></red>'
  scorecard:
    header: '<gold>Scorecard - <course></gold>'
    row: '<gray>Hole <white><hole></white> <dark_gray>|</dark_gray> par <white><par></white> <dark_gray>|</dark_gray> <white><strokes></white></gray>'
    total: '<gold>Total: <white><strokes></white> (<diff>)</gold>'
  course:
    unknown: '<red>No course called <course>.</red>'
    none: '<gray>No playable courses yet.</gray>'
    list-header: '<gold>Courses</gold>'
    list-row: '<gray><dark_gray>-</dark_gray> <white><id></white> <dark_gray>|</dark_gray> <display> <dark_gray>|</dark_gray> <holes> holes, par <par></gray>'
  leaderboard:
    header: '<gold>Leaderboard - <course></gold>'
    row: '<gray><white><rank>.</white> <player> <dark_gray>|</dark_gray> <white><strokes></white> (<diff>)</gray>'
    empty: '<gray>Nobody has finished <course> yet.</gray>'
    error: '<red>Could not read the leaderboard.</red>'
  admin:
    course-created: '<green>Created course <white><course></white> in world <white><world></white>.</green>'
    course-exists: '<red>Course <course> already exists.</red>'
    course-selected: '<green>Editing course <white><course></white>.</green>'
    course-deleted: '<green>Deleted course <course>.</green>'
    course-in-use: '<red>Course <course> has a round in progress.</red>'
    no-course-selected: '<red>Select a course first: /puttputt admin select <course></red>'
    tee-set: '<green>Tee for hole <hole> set to your position.</green>'
    cup-set: '<green>Cup for hole <hole> set to your position.</green>'
    par-set: '<green>Hole <hole> is now par <par>.</green>'
    corner-set: '<green>Corner <corner> set to <x>, <y>, <z>.</green>'
    bounds-set: '<green>Bounds for hole <hole> set from your selection.</green>'
    no-selection: '<red>Set both corners first: /puttputt admin pos1 and pos2.</red>'
    no-such-hole: '<red>Hole <hole> does not exist on this course.</red>'
    hole-deleted: '<green>Deleted hole <hole>.</green>'
    unknown-material: '<red>Unknown material <material>.</red>'
    unknown-surface: '<red>Unknown surface <surface>.</red>'
    surface-global: '<green>Mapped <material> to <surface> globally.</green>'
    surface-hole: '<green>Mapped <material> to <surface> on hole <hole>.</green>'
    saved: '<green>Courses saved.</green>'
    save-failed: '<red>Could not save courses - see console.</red>'
    reloaded: '<green>Config and courses reloaded.</green>'
    info: '<gray>Parties: <white><parties></white> <dark_gray>|</dark_gray> courses: <white><courses></white> <dark_gray>|</dark_gray> active rounds: <white><rounds></white> <dark_gray>|</dark_gray> economy: <white><economy></white></gray>'
